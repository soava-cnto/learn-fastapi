installer .venv
py -3 -m venv .venv

activer .venv
.venv\Scripts\Activate.ps1

Lancer le serveur
uvicorn main:app --reload

# main → correspond au nom du fichier (main.py)

# app → correspond à l’instance de FastAPI qu’on a crééegit